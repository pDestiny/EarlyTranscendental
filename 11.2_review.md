⸻

📘 11.2 극한과 연속 — 복습 정리 & 연습문제

⸻

1. 이변수 함수의 극한
•	정의
$f$가 $(a,b)$ 근처에서 정의되어 있을 때,
$$
\lim_{(x,y)\to(a,b)} f(x,y) = L
$$
이라는 것은, 임의의 $\epsilon > 0$에 대해 $\delta > 0$이 존재하여
$0 < \sqrt{(x-a)^2+(y-b)^2} < \delta$이면 $|f(x,y)-L| < \epsilon$이 성립함을 의미한다.
•	핵심 포인트
•	일변수 함수에서는 왼쪽/오른쪽 두 방향만 고려하면 되지만, 이변수 함수는 무한히 많은 경로로 접근 가능.
•	따라서 모든 경로에서 같은 극한값이 나와야 극한이 존재.
•	서로 다른 두 경로에서 다른 값이 나오면 극한은 존재하지 않는다.

**연습문제**
1.	$\lim_{(x,y)\to (0,0)} (x^2+y^2)$를 구하라.

**풀이**

임의의 $\epsilon > 0$를 잡고 $\delta = \sqrt{\epsilon}$로 두자.

만약 $0 < \sqrt{(x - 0)^2 + (y - 0)^2} < \delta$라면, 즉 $0 < \sqrt{x^2 + y^2} < \sqrt{\epsilon}$이므로 양변을 제곱해서 $x^2 + y^2 < \epsilon$. 따라서 $|(x^2 + y^2) -0| = x^2 + y^2 < \epsilon$이 성립한다. 이는 임의의 $\epsilon > 0$에 대해 적당한 $\delta > 0$(여기선 $\sqrt{\epsilon}$)가 존재함을 보였음으로 $\lim_{(x, y) \to (0, 0)}(x^2 + y^2)= 0$임을 증명된다.

2.	$\lim_{(x,y)\to (0,0)} \dfrac{2x^2}{x^2+y^2}$가 존재하는지 판별하라.

**풀이**

$x = 0, y \to 0$이라 가정했을 때, $\lim_{y \to 0}\frac{0}{y^2} = 0$이다.

$x \to 0, y = 0$이라 가정했을 때, $\lim_{x \to 0}\frac{2x^2}{x^2} = 2$이다.

따라서, 서로 다른 방향에서의 극한 값이 다름으로 $\lim_{(x, y) \to (0, 0)}$은 존재하지 않는다.

3.	$\lim_{(x,y)\to (0,0)} \frac{x^2y}{x^4+y^2}$의 극한 존재 여부를 경로별로 검토하고 결론을 내려라.

**풀이**
$y = x^2$일 경우 $\frac{x^4}{2x^4} = \frac{1}{2}$이다.

$x = 0, y \to 0$일 경우 $\lim_{y \to 0}\frac{0}{y^2} =0$이다.

따라서 서로 다른 경로에서 극한 값이 다름으로 $\lim_{(x,y)\to (0,0)} \frac{x^2y}{x^4+y^2}$의 극한은 존재하지 않는다.

⸻

2. 극한이 존재하지 않는 경우
•	아이디어
•	어떤 방향(예: $x$축, $y$축, $y=x$)으로 접근했을 때 극한값이 달라지면 극한은 존재하지 않는다.
•	경로별 계산으로 모순을 찾는 것이 중요.

**연습문제**
1.	$\lim_{(x,y)\to(0,0)} \frac{x-y}{x+y}$의 존재 여부를 확인하라.

**풀이**

$x =0, y\to 0$이라 가정 했을 때, $\lim_{y \to 0}\frac{-y}{y} = -1$이다.
$x \to 0, y = 0$이라 가정 했을 때, $\lim_{x \to 0}\frac{x}{x} = 1$이다.

$x, y$축 방향으로 접근 했을 때의 극한 값이 서로 다르므로 $\lim_{(x,y)\to(0,0)} \frac{x-y}{x+y}$의 값은 존재하지않는다.

2.	$f(x,y)=\dfrac{x^2-y^2}{x^2+y^2}$의 $(0,0)$에서 극한이 존재하지 않음을 보이라.

연습문제 1번과 마찬가지로 $x, y$축으로의 접근 시 극한값이 다르다. $y$축으로 접근 시에는 극한 값이 $-1$인데 반해 $x$축으로 접근 시에는 극한 값은 1로 서로 다른 극한 값을 갖는다.

3.	$f(x,y)=\dfrac{x^3}{x^2+y^2}$의 $(0,0)$에서 극한이 존재하는지, 극한이 존재하지 않는다면 어떤 경로에서 다른 값이 나오는지 설명하라.

**풀이**

$$0 \le \left|\frac{x^3}{x^2 + y^2} \right| \le \frac{|x|^3}{x^2} = |x| \le \sqrt{x^2 + y^2} \to_{(x, y) \to (0,0)}0$$

따라서 조임 정리에 의해 $0\le \left|\frac{x^3}{x^2 + y^2} \right| \le \sqrt{x^2 + y^2}$이다. 그러므로 $\lim_{(x, y) \to (0,0)}\frac{x^3}{x^2 + y^2}$은 극한이 존재한다.


⸻

3. 극한의 $\epsilon$–$\delta$ 정의 적용
•	핵심
•	극한값을 증명할 때는 $\epsilon$에 대응하는 $\delta$를 제시해야 한다.
•	보통 부등식을 정리해 $\sqrt{x^2+y^2}$에 대한 조건으로 변환한다.

**연습문제**
1.	$\lim_{(x,y)\to(0,0)} xy = 0$임을 $\epsilon$–$\delta$ 정의로 보이라.

**풀이**

$r = \sqrt{x^2 + y^2}$이라 두면 $|x| \le r, |y| \le r$이므로 $|xy| = |x||y|\le r^2$. 임의의 $\epsilon > 0$에 대해 $\delta = \sqrt{\epsilon}$로 두면 $0 < \sqrt{x^2 + y^2} = r < \delta \Rightarrow |xy| \le r^2 < \delta^2 = \epsilon$. 따라서 $\lim_{(x,y) \to (0, 0)}xy = 0$

2. $\lim_{(x,y)\to(0,0)} \dfrac{3x^2y}{x^2+y^2}=0$임을 증명하라.



**풀이**

$r = \sqrt{x^2 + y^2}$이라 두면 $|x| \le r, |y| \le r$이므로 $\left|\dfrac{3x^2y}{x^2+y^2}\right| \le \left|\dfrac{3r^3}{r^2} \right| = |r|$이다. 임의의 $\epsilon > 0$에 대해 $\delta = \dfrac{1}{3}\epsilon$으로 두면 $0 < \sqrt{x^2 + y^2} = r < \delta \Rightarrow \left|\dfrac{3x^2y}{x^2+y^2}\right| = \left|3r\right| < 3\delta = \epsilon$이다. 따라서 $\lim_{(x,y)\to(0,0)} \dfrac{3x^2y}{x^2+y^2}=0$이다.

3.	$\lim_{(x,y)\to(0,0)} \dfrac{4xy}{x^2+y^2}$의 극한 존재 여부를 $\epsilon$–$\delta$ 정의를 활용하여 설명하라.

**참조**

AM-GM(Arithmetic Mean - Geometric Mean Inequation) 부등식 이란? 

산술평균과 기하평균 사이의 상관관계를 나타내는 고전적인 부등식.

* 산술 평균 : $AM = \dfrac{a + b}{2}$
* 기하 평균 : $GM = \sqrt{ab}$

AM-GM 부등식은 모든 양수 $a,b > 0$에 대하여 $\dfrac{a + b}{2} \ge \sqrt{ab}$가 성립한다.

예를 들어 $x^2 =a, y^2 = b$라고 하였을 때, $\dfrac{x^2 + y^2}{2} \ge |xy|$가 성립한다. 등호가 성립하는 경우는 오로지 $x = y$일 경우이다.


**풀이**
$f(x,y)=\dfrac{4xy}{x^2+y^2}$라 하자. 극한 L이 존재한다고 가정하자. 그러면

임의의 $\epsilon>0$에 대해 어떤 $\delta>0$가 존재해서
$0<\sqrt{x^2+y^2}<\delta \Rightarrow |f(x,y)-L|<\epsilon$.

이제 \epsilon=1로 고정한다. 가정에 따라 어떤 $\delta>0$가 존재한다.
•	$t=\dfrac{\delta}{2\sqrt{2}}$ 로 두자(양수).
그러면 $(t,t)$와 $(t,-t)$ 모두
$\sqrt{t^2+t^2}=t\sqrt{2}=\dfrac{\delta}{2}<\delta$ 이므로 가정의 조건을 만족한다.

이 두 경로에서 함수값은
$$f(t,t)=\frac{4t^2}{2t^2}=2,\qquad
f(t,-t)=\frac{-4t^2}{2t^2}=-2$$.
따라서
$$|2-L|<1 \quad |-2-L|<1$$
이 동시에 성립해야 한다. 그런데 이 두 부등식은 함께 참일 수 없어(서로 중심이 2와 -2인 반지름 1 구간은 겹치지 않음; 두 점 사이 거리는 4). 모순이므로 처음의 가정(극한 존재)은 거짓.
⸻

4. 이변수 함수의 연속
•	정의
$$
\lim_{(x,y)\to(a,b)} f(x,y) = f(a,b)
$$
가 성립하면 $f$는 $(a,b)$에서 연속이다.
•	사실
•	모든 다항함수는 $\mathbb{R}^2$에서 연속.
•	유리함수는 분모가 0이 아닌 점에서 연속.

**연습문제**
1.	$f(x,y)=x^2+y^2$가 $(1,2)$에서 연속임을 확인하라.

**풀이**

$\lim_{(x, y) \to (1, 2)}x^2 + y^2$의 경우 극한값이 존재하며 5이다. $f(1,2) = 5$임으로 $\lim_{(x, y) \to (1,2)}f(x, y) = f(1, 2)$와 같음으로 연속이다.

2.	$f(x,y)=\dfrac{x^2}{x^2+y^2}$의 정의역과 연속성을 판별하라.

**풀이**
$\{(x, y) | x \ne 0 \ \cap y \ne 0\}$인 $(x,y)$쌍을 정의역으로 가지며 유리함수는 분모가 0이 아닌 곳에서 항상 연속임으로 $f$의 정의역 위의 모든 점에서 $f$는 연속이다.

3.	$f(x,y)=\begin{cases} \dfrac{xy}{x^2+y^2}, & (x,y)\neq (0,0) \\ 0, & (x,y)=(0,0) \end{cases}$가 $(0,0)$에서 연속인지 판별하라.

**풀이**
연속이 아니다. 왜냐하면 $x = y$ 방향에서 접근 하였을 때, $\lim_{x \to 0}\dfrac{x^2}{2x^2} = \dfrac{1}{2}$이나 $f(0, 0) = 0$임으로 $(0, 0)$에서 연속하지 않는다.



⸻

5. $n$변수 함수의 일반화
•	정의
$f:\mathbb{R}^n\to \mathbb{R}$일 때,
$$
\lim_{\mathbf{x}\to \mathbf{a}} f(\mathbf{x})=L
$$
은 임의의 $\epsilon >0$에 대해 $\delta>0$가 존재하여
$0<|\mathbf{x}-\mathbf{a}|<\delta \implies |f(\mathbf{x})-L|<\epsilon$이 성립함을 뜻한다.

**연습문제**
1.	$\lim_{(x,y,z)\to (0,0,0)} (x^2+y^2+z^2)$를 구하라.

**풀이**

Polynomial 함수는 연속함으로 $\lim_{(x, y, z) \to (0, 0, 0)}x^2 + y^2 + z^2 = 0$이다.

2.	$\lim_{(x,y,z)\to (0,0,0)} \dfrac{x^2+y^2}{x^2+y^2+z^2}$가 존재하는지 판별하라.

**풀이**

유리함수는 분모가 $0$이 되지 않는 지점에서 연속이다. $x \to 0, y\to 0, z = 0$일 경우 $\lim_{(x, y) \to (0, 0)}\frac{x^2 + y^2}{x^2 + y^2} = 1$이나 $x = 0, y = 0, z \to 0$일 경우 $\lim_{z \to 0}0/z^2 = 0$임으로 서로 다른 방향에서 극한 값이 다르다.


3.	$\lim_{(x,y,z)\to (0,0,0)} \dfrac{xyz}{x^2+y^2+z^2}$의 극한 존재 여부를 다양한 경로로 검토하라.

**풀이**

⸻