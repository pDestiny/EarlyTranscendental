⸻

# 🎯 개념 정리 요약

## 1️⃣ 문제의 형태

제약 조건이 있는 최적화 문제는 다음과 같은 형태야:
$$\max_{x,y,\ldots} f(x,y,\ldots) \quad \text{subject to} \quad g(x,y,\ldots) = 0$$

## 2️⃣ 기본 원리

극값이 나오는 점에서는 두 기울기가 평행해.
즉,
$$\nabla f = \lambda \nabla g$$
이걸 만족하는 λ(라그랑주 곱수) 가 존재한다는 게 핵심이야.

## 3️⃣ 라그랑주 함수 정의

$$\mathcal{L}(x, y, \lambda) = f(x, y) - \lambda g(x, y)$$
제약이 여러 개면 λ 대신 λ₁, λ₂, … 를 써서 각각 대응시켜.

## 4️⃣ 해법 절차
1.	라그랑주 함수 $\mathcal{L}$ 작성
2.	각 변수 및 λ에 대해 편미분 → 0으로 두기
3.	연립방정식 풀기 → $x, y, \lambda$ 구하기
4.	$f(x, y)$에 대입해서 최대/최소 판단

## 5️⃣ 기하학적 의미

등위선 $f(x,y) = c$과 제약식 $g(x,y) = k$이 접하는 점에서 극값이 생긴다.
이때 두 함수의 법선(기울기 벡터)이 평행하다는 의미지.

⸻

🧩 연습문제 5개

## 문제 1️⃣ (기본)

$f(x, y) = x^2 + y^2$를 제약식 $x + 2y = 6$ 하에서 최소화하라.

**풀이**

$$
\begin{align}
\mathcal{L}(x, y) &= x^2 + y^2 - \lambda(x + 2y - 6) \\
\frac{\partial \mathcal{L}}{\partial{x}} &= 2x - \lambda = 0 \\
\frac{\partial \mathcal{L}}{\partial{y}} &= 2x - 2\lambda = 0 \\
\frac{\partial \mathcal{L}}{\partial{\lambda}} &= x + 2y - 6 = 0
\end{align}
$$

위의 연립방정식을 풀면 $x = \dfrac{\lambda}{2}, y=\lambda$가 됨으로 $x + 2y - 6 = 0 \Rightarrow \dfrac{5}{2}\lambda = 6$이 됨으로 따라서 $\lambda = \dfrac{12}{5}$이다. 이를 $x, y$에 적용하면 $x = \dfrac{6}{5}, y = \dfrac{12}{5}$다.

⸻

## 문제 2️⃣ (곡선 제약)

$f(x, y) = xy$를 $x^2 + y^2 = 8$ 위에서 최대화하고 최소화하라.

**풀이**

$$
\begin{align}
\mathcal{L}(x, y) &= xy - \lambda(x^2 + y^2 - 8) \\
\frac{\partial \mathcal{L}}{\partial{x}} &= y - 2x\lambda = 0 \\
\frac{\partial \mathcal{L}}{\partial{y}} &= x - 2y\lambda = 0 \\
\frac{\partial \mathcal{L}}{\partial{\lambda}} &= x^2 + y^2 -8 = 0
\end{align}
$$

위의 연립방정식을 풀면 $\lambda = \dfrac{y}{2x} = \dfrac{x}{2y}$, 즉 $\dfrac{x}{y} = \dfrac{y}{x} \Rightarrow |x| = |y|$라는 결론에 이르게 된다. 이를 $x^2 + y^2 - 8 = 0$에 대입해 보면 $|x| = |y| = \pm|2|$이다. 총 4개의 조합이 나올 수 있고 이에 따라 조건에 만족하는 $f(x,y)$의 최대값은 $4$, 최소값은 $-4$가 된다.


⸻

## 문제 3️⃣ (공간 문제)

$f(x, y, z) = xyz$를 $x + y + z = 9$ 하에서 최대화하라.

**해설**
라그랑주 문제(등산로)를 풀기 위해 이계 편도함수 판정법(봉우리 찾기)을 먼저 쓰는 것은, 애초에 엉뚱한 곳을 수색하는 것이며, 이것이야말로 오히려 손발이 고생하는 헛수고입니다.
​그렇다면 라그랑주 승수법을 쓰기 전 최대/최소값이 존재하는지 확인하는 올바른 점검은 무엇일까요?
​바로 최대-최소 정리입니다.
​라그랑주 승수법을 적용하기 전에 우리가 점검해야 할 '필수 조건'은 다음 두 가지입니다.
​우리가 최대화하려는 함수 $f(x,y)$가 연속인가?
​우리가 걷는 등산로(제약 조건 $g(x,y)=k$)가 닫혀있고 유계인(Closed and Bounded) 영역인가? (예: 원, 타원, 사각형 등)
​이 두 조건이 만족되면, 최대/최소값은 반드시 존재함이 수학적으로 보장됩니다.
​그때야 비로소 우리는 손발이 고생할 준비, 즉 라그랑주 승수법 계산을 시작하는 것이죠.
점검은 필수입니다. 단, 그 점검은 이계 편도함수 판정법이 아니라, 최대-최소 정리(EVT)'여야 합니다.

⸻

## 문제 4️⃣ (두 개의 제약)

$f(x, y, z) = x + 2y + 3z$를
$$
\begin{cases}
x + y + z = 6 \\
x - y + z = 2
\end{cases}
$$
라는 두 제약식 하에서 구하라.

**풀이**




⸻

## 문제 5️⃣ (응용)

반지름이 $4$인 구면 위 $x^2 + y^2 + z^2 = 16$에서
점 $(2, -2, 1)$에 가장 가까운 점을 찾아라.
(즉, 거리 최소화 문제 — 고전적인 라그랑주 응용!)

**풀이**

구면의 점 $(x, y, z)$와 점 $(2, -2, 1)$과의 거리는 법선 벡터가 $\langle 2, -2, 1 \rangle$임으로 다음과 같다.

$$
f(x, y, z) = \frac{|2x - 2y + z|}{\sqrt{4 + 4 + 1}} = \frac{|2x - 2y + z|}{3}
$$

따라서 제약 사향인 구면 $g(x, y, z) = x^2 + y^2 + z^2 - 16$의 라그랑주 곱수를 이용하면 아래와 같이 풀이 할 수 있다.

$$
\begin{align}
\mathcal{L}(x, y, z) &= \frac{|2x - 2y + z|}{3} - \lambda(x^2 + y^2 + z^2 - 16) \\
\frac{\partial{\mathcal{L}}}{\partial {x}} &= \frac{2}{3} - 2\lambda x = 0 \\
\frac{\partial{\mathcal{L}}}{\partial {y}} &= \frac{2}{3} + 2\lambda y = 0 \\
\frac{\partial{\mathcal{L}}}{\partial {z}} &= \frac{1}{3} - 2\lambda z = 0 \\
\frac{\partial{\mathcal{L}}}{\partial {\lambda}} &= x^2 + y^2 + z^2 - 16 = 0
\end{align}
$$

위의 식을 풀어보면 $x = \dfrac{1}{3\lambda}, y = -\dfrac{1}{3\lambda}, z= \dfrac{1}{6\lambda}$임을 알 수 있고 $\lambda$에 대한 편미분에 적용해 보면 $\lambda = \pm \dfrac{1}{8}$임을 알 수 있다. 

$\dfrac{|2x - 2y + z|}{3}$에서 $\lambda = -\dfrac{1}{8}$이면 거리는 $\dfrac{1}{144}$이고 $\lambda = \dfrac{1}{8}$일 경우에는 거리는 $\dfrac{5}{144}$ 이다. 그러므로 가장 가까운 점 $(x, y, z)$ 는 $(-\frac{1}{24}, \frac{1}{24}, -\frac{1}{48})$이다.

⸻