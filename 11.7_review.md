# 🧭 11.7 최댓값과 최솟값 요약

⸻

## 1️⃣ 극대와 극소의 정의

정의:
이변수 함수 $f(x, y)$가 점 $(a,b)$에서
	•	극대이면, 근방의 모든 $(x,y)$에 대해 $f(x,y) \le f(a,b)$
	•	극소이면, 근방의 모든 $(x,y)$에 대해 $f(x,y) \ge f(a,b)$

즉, $(a,b)$ 근처에서 함수가 그 점에서 가장 크거나 작을 때를 말해.

⸻

### ✅ 임계점(Critical Point)

$f_x(a,b)=0$이고 $f_y(a,b)=0$이거나, 두 편미분 중 하나가 존재하지 않을 때 $(a,b)$를 임계점이라 해.

임계점은 극값이 될 수도 있고, 아닐 수도 있다.

⸻

🧮 **연습문제 1**

$f(x, y) = x^2 + y^2 - 4x - 6y + 13$의 극값을 구하라.

힌트:
$f_x = 2x - 4$, $f_y = 2y - 6$ → 임계점 $(2, 3)$
이 점이 극솟값인지 확인해봐.

**풀이**

$x = 2, y = 3$일 때 $f_x = f_y = 0$이 되며 임계점은 $(2, 3)$이 된다. 극값을 확인하기 위해서는 주변 값을 확인해 봐야 하기 때문에 $x, y$중 하나를 고정하여 값을 줄여보거나 늘려봄으로써 극값인지를 확인 할 수 있다.

```julia
f(x, y) = x^2 + y^2 - 4x - 6y + 13
f(2, 3) # 0
f(1, 3) # 1
f(2, 2) # 1
f(3, 3) # 1
f(2, 4) # 1
```
주변 값에 비해 임계점이 가장 작은 것을 확인 할 수 있어 $(2, 3)$이 극값이다.

⸻

## 2️⃣ 이계 편도함수 판정법 (Second Derivative Test)

임계점 $(a,b)$에서
$$
D = f_{xx}(a,b)f_{yy}(a,b) - (f_{xy}(a,b))^2
$$

이면 다음과 같이 판정한다:
	•	(a) $D > 0$, $f_{xx} > 0$ → 극솟값
	•	(b) $D > 0$, $f_{xx} < 0$ → 극댓값
	•	(c) $D < 0$ → 안장점(saddle point)
	•	(d) $D = 0$ → 판정 불가

⸻

🧮 **연습문제 2**

$f(x, y) = x^3 - 3xy^2$
임계점을 구하고, 각각의 점이 극댓값, 극솟값, 또는 안장점인지 판정하라.

**풀이**

$$
\begin{align}
f_x &= 3x^2 - 3y^2 \\
f_y &= -6xy
\end{align}
$$

따라서 임계점은 $f_x = f_y = 0$이 되는 지점은 $(x + y)(x-y) + 6xy = 0$이 성립되는 지점임으로 $(0, 0)$이다.

$$
\begin{align}
f_{xx} &= 6x \\
f_{yy} &= -6x \\
f_{xy} &= -6y \\
D(x, y) &= f_{xx}f_{yy} - (f_{xy})^2 \\
&= -36(y^2 + x^2)
\end{align}
$$

임계점인 $(0, 0)$에서의 극값은 이계편도 함수 판정법으로는 $D=0$임으로 판정이 불가하다.

⸻

## 3️⃣ 닫히고 갇힌 집합에서의 최대·최소 정리

$f$가 닫히고 갇힌 영역 $D$에서 연속이면,
$f$는 반드시 $D$ 내에서 최대값과 최솟값을 가진다.

닫힌 집합: 경계점을 포함하는 집합
갇힌 집합(유계): 유한한 범위 안에 포함되는 집합

예:
$$D = \{(x,y) | x^2 + y^2 \le 1\}$$
(원의 내부와 경계 모두 포함 → 닫히고 갇힘)

⸻

📋 **최대·최소 찾기 단계**
	1.	$D$ 내부의 임계점에서 $f$값 계산
	2.	$D$의 경계 위에서 $f$의 극값 계산
	3.	둘 중 가장 큰 값 = 최댓값, 가장 작은 값 = 최솟값

⸻

🧮 **연습문제 3**

영역 $D = \{(x, y) | 0 \le x \le 2, 0 \le y \le 2\}$에서
$f(x, y) = x^2 + y^2 - 2x - 2y$의 최대값과 최소값을 구하라.

**풀이**

```julia
f(x, y) = x^2 + y^2 - 2x - 2y

# f_x(x, y) = 2x - 2
# f_y(x, y) = 2y - 2
# x, y = (1, 1)에서 임계점을 가진다.
# f_xx = 2, f_yy = 2 이고 f_xy = 0임으로 D > 0이고 f_xx > 0임으로 극솟값이 존재한다.

min(f(1, 1), f(0, 2), f(2, 0), f(0, 0), f(2, 2)) # -2
max(f(1, 1), f(0, 2), f(2, 0), f(0, 0), f(2, 2)) # 0
```

따라서 $D$영역에서의 최솟값은 $-2$이고 최댓값은 $0$이다.
⸻

## 4️⃣ 거리의 최소화 문제 (응용)

점 $(x_0, y_0, z_0)$과 평면 $ax + by + cz + d = 0$ 사이의 거리:

$$
d = \frac{|ax_0 + by_0 + cz_0 + d|}{\sqrt{a^2 + b^2 + c^2}}
$$

이 공식은 라그랑주 승수법 없이 바로 적용 가능하지만,
미적분적으로 풀 때는 $d^2$를 최소화하는 문제로 바꿔서 푼다.

⸻

🧮 **연습문제 4**

점 $(1, 2, -1)$에서 평면 $2x - y + 2z - 3 = 0$까지의 거리를 구하라.

**풀이**

```julia

d(x, y, z) =abs(2x - y + 2z - 3) / sqrt(2^2 + 1 + 2^2)
d(1, 2, -1) # 5/3
```

⸻

🌟 요약 포인트

구분	조건	결과
$D > 0$, $f_{xx} > 0$	접평면이 위로 볼록	극솟값
$D > 0$, $f_{xx} < 0$	접평면이 아래로 볼록	극댓값
$D < 0$	안장점	극값 없음
닫히고 갇힌 영역	연속함수는 극값 가짐	최대·최소 존재


⸻