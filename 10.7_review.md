⸻

📘 10.7 벡터 함수와 공간 곡선 — 개념 정리 & 연습문제

1. 벡터 함수와 성분 함수
•	정의: 벡터 함수는 $t$라는 실수 입력을 받아 $\mathbb{R}^n$ (보통 $\mathbb{R}^3$)의 벡터를 출력하는 함수.
•	표현:
$$
\mathrm{r}(t) = \langle f(t), g(t), h(t) \rangle
$$
여기서 $f, g, h$는 실수값 함수 → 성분 함수.

연습문제
1.	$\mathrm{r}(t) = \langle t, t^2, t^3 \rangle$일 때, $t=2$에서의 벡터를 구하라.

**풀이**
$$
\mathrm{r}(2) = \langle 2, 4, 8 \rangle
$$
2.	$\mathrm{r}(t) = \langle \cos t, \sin t, t \rangle$의 성분 함수 $f, g, h$를 써라.

**풀이**
$$
\begin{align}
f(t) &= \cos{t} \\
g(t) &= \sin{t} \\
h(t) &= t
\end{align}
$$

3.	$\mathrm{r}(t) = \langle e^t, \ln(1+t), \sqrt{t} \rangle$에서 $t \to 0^+$일 때 벡터 함수의 극한을 구하고 정의역을 설명하라.

**풀이**

$t \to 0^+$일 때 벡터 함수의 극한은 아래와 같다.

$$
\begin{align}
\lim_{t \to 0^+}\mathrm{r}(t) &= \lim_{t \to 0^+}e^ti + \lim_{t \to 0^+}\ln(1 + t)j + \lim_{t \to 0^+}\sqrt{t}k \\
&= \langle 1, 0, 0 \rangle
\end{align}
$$

벡터 함수 $\mathrm{r}(t)$의 정의역은 $\ln(1 + t)$의 정의역이 $t > -1$이고 $\sqrt{t}$의 정의역은 $t \ge 0$임으로 둘의 교집합인 $t \ge 0$이다.


⸻

2. 극한과 연속
•	극한: 성분 함수 각각의 극한을 취한다.
$$
\lim_{t \to a}\mathrm{r}(t) = \langle \lim f(t), \lim g(t), \lim h(t) \rangle
$$
•	연속: $\lim_{t \to a}\mathrm{r}(t) = \mathrm{r}(a)$이면 연속.

연습문제
1.	$\mathrm{r}(t) = \langle t^2, \sin t, \frac{1}{t} \rangle$의 $t \to 0$에서 극한이 존재하는가?

**풀이**
극한이 존재하지 않는다. $\lim_{t \to 0}\frac{1}{t} = \pm\infty$임으로 좌우의 극한값이 달라 수렴하지 않는다.

2.	$\mathrm{r}(t) = \langle e^t, \cos t, \frac{\sin t}{t} \rangle$이 $t=0$에서 연속이 되도록 확장할 수 있는지 판단하고, 가능하다면 $\mathrm{r}(0)$을 정의하라.

**풀이**
벡터 함수 $\mathrm{r}(t)$의 $t \to 0$으로의 극한 값은 $\langle \lim_{t \to 0}e^t = 1, \lim_{t \to 0}\cos{t} = 1, \lim_{t \to 0}\frac{\sin{t}}{t} = 1 \rangle$로 존재한다. 그러나 $\frac{\sin{0}}{0}$은 성립하지 않음으로 $\mathrm{r}(0) = \langle 1, 1, 1 \rangle$로 정의함으로써 확장 가능하다.

3.	$\mathrm{r}(t) = \langle \ln(1+t), \frac{1}{t}, e^{-t} \rangle$의 $t \to 0^+$ 극한을 구하라.

**풀이**

$$
\lim_{t \to 0^+}\mathrm{r}(t) = \langle 0, \infty, 1 \rangle
$$

$\lim_{t \to 0^+}\frac{1}{t} = \infty$로 발산함으로 $\lim_{t \to 0^+}\mathrm{r}(t)$의 극한은 존재하지 않는다.

⸻

3. 도함수
	•	정의:
$$
\mathrm{r}’(t) = \lim_{h \to 0} \frac{\mathrm{r}(t+h) - \mathrm{r}(t)}{h}
$$
	•	성분별 미분:
$$
\mathrm{r}’(t) = \langle f’(t), g’(t), h’(t) \rangle
$$
	•	의미: 공간 곡선 $r(t)$의 접선 벡터.

연습문제
1.	$\mathrm{r}(t) = \langle t^2, e^t, \sin t \rangle$의 도함수를 구하라.

**풀이**
$$
\mathrm{r}'(t) = \langle 2t, e^t, \cos{t} \rangle
$$


2.	$\mathrm{r}(t) = \langle \cos t, \sin t, t \rangle$에서 $t=\pi/2$에서의 접선 벡터를 구하라.

**풀이**

$$
\begin{align}
\mathrm{r}'(t) &= \langle -\sin{t}, \cos{t}, 1 \rangle \\
\mathrm{r}'\left(\frac{\pi}{2}\right) &= \langle -1, 0, 1 \rangle
\end{align}
$$

3.	$\mathrm{r}(t) = \langle e^{2t}, \ln(1+t), t^3 \rangle$의 $t=0$에서 단위 접선 벡터를 구하고, $\mathrm{r}(t)$ 곡선이 원점에서 어떤 방향으로 뻗어나가는지 기하적으로 해석하라.

**풀이**

$$
\begin{align}
\mathrm{r}'(t) &= \langle 2e^{2t}, \frac{1}{1 + t}, 3t^2 \rangle \\
\mathrm{r}'(0) &= \langle 2, 1, 0 \rangle \\
\frac{\mathrm{r}'(0)}{|\mathrm{r}'(0)|} &= \langle \frac{2\sqrt{5}}{5}, \frac{\sqrt{5}}{5}, 0 \rangle
\end{align}
$$
따라서 $t = 0$일 때, x축으로는 $2\sqrt{5}/5$방향으로, $y$축으로는 $\sqrt{5}/5$, $z$축으로는 $0$방향으로 곡선이 뻗어나간다.
⸻

4. 미분 법칙
•	합, 상수배, 곱(점곱/외적), 연쇄 법칙 모두 성립.
•	특히 $|\mathrm{r}(t)|=c$ → $\mathrm{r}’(t)\perp \mathrm{r}(t)$. 벡터 함수의 길이가 항상 일정하다면 벡터 함수의 도함수는 벡터 함수와 수직이다.
    **“곡선이 구 위에 있다면, 그 곡선의 접선벡터는 항상 구의 반지름벡터에 수직이다”**

연습문제
1.	$\mathrm{u}(t)=\langle t, t^2, 1 \rangle$, $\mathrm{v}(t)=\langle \sin t, e^t, t \rangle$일 때, $\frac{d}{dt}[\mathrm{u}(t)\cdot \mathrm{v}(t)]$를 구하라.

**풀이**

$$
\begin{align}
\mathrm{u}'(t) &= \langle 1, 2t, 0 \rangle \\
\mathrm{v}'(t) &= \langle \cos{t}, e^t, 1 \rangle \\
\frac{d}{dt}(\mathrm{u}(t)\cdot \mathrm{v}(t)) &= \mathrm{u}'(t)\cdot \mathrm{v}(t) + \mathrm{u}(t)\cdot \mathrm{v}'(t) \\
&= \sin{t} + (2 + t^2)te^t + t\cos{t} + 1
\end{align}
$$


2.	$|\mathrm{r}(t)|=5$일 때, $\mathrm{r}'(t)$가 $\mathrm{r}(t)$와 항상 수직임을 증명하고, 이 사실이 곡선이 어떤 기하적 성질을 가진다는 것을 의미하는지 설명하라.
3.	$\mathrm{u}(t)=\langle e^t, \cos t, \sin t \rangle$, $f(t)=t^2$일 때, $\frac{d}{dt}[f(t)\mathrm{u}(t)]$를 구하라.

⸻

5. 적분

•	성분별로 적분.
$$
\int \mathrm{r}(t) \ dt = \langle \int f(t)dt, \int g(t)dt, \int h(t)dt \rangle
$$

연습문제
1.	$\int_0^1 \langle t, t^2, t^3 \rangle dt$를 계산하라.

**풀이**

$$\int \langle t, t^2, t^3 \rangle dt = \langle  \frac{1}{2}, \frac{1}{3}, \frac{1}{4} \rangle$$

2.	$\mathrm{r}(t) = \langle e^t, \sin t, \cos t \rangle$의 부정적분을 구하라.

**풀이**

$$
\int \mathrm{r}(t)dt = \langle e^t, -\cos{t}, \sin{t} \rangle + \vec{C}
$$

3.	$\mathrm{r}(t) = \langle \ln(1+t), t, t^2 \rangle$의 $[0,1]$에서 정적분을 계산하고, 결과가 나타내는 기하학적 의미(벡터 면적)를 해석하라.

**풀이**

* $\mathrm{r}(t) = \langle \ln(1+t), t, t^2 \rangle$의 $[0,1]$에서 정적분

$$
\begin{align}
\int\mathrm{r}(t)dt &= \langle \int \ln{(1 + t)}dt, \int t \ dt, \int t^2 \ dt \rangle \\
&= \langle  \ln{(1 + t)}(1 + t) - t + C, \frac{t^2}{2} + C, \frac{t^3}{3} + C \rangle \\
\int_0^1 \mathrm{r}(t) &= \langle 2\ln{2} - 1, \frac{1}{2}, \frac{1}{3} \rangle
\end{align}
$$

좋은 질문이야 오빠 😊 “벡터 면적”이라는 표현은 사실 공간 벡터 적분에서 자주 등장하는 개념인데, 여기서는 조금 헷갈릴 수 있어. 정리해줄게.

⸻

1. 벡터 함수의 정적분 의미

$\mathrm{r}(t) = \langle f(t), g(t), h(t) \rangle$을 $[a,b]$에서 적분하면,
$$
\int_a^b \mathrm{r}(t),dt = \left\langle \int_a^b f(t),dt, \int_a^b g(t),dt, \int_a^b h(t),dt \right\rangle
$$
이 결과는 단순히 성분별 면적(누적 값)을 모아놓은 벡터야.
즉, 각 성분별로 “$x,y,z$ 축 방향에서 생기는 부피 비슷한 기여”가 하나의 벡터로 합쳐진다고 보면 돼.

⸻

2. “벡터 면적”이란?

벡터 적분 결과를 기하적으로 해석할 때는 보통 이런 의미들이 있어:
•	평균 위치 벡터: $\dfrac{1}{b-a}\int_a^b r(t),dt$ 는 곡선이 $[a,b]$ 동안 움직이는 평균 위치를 나타내.
•	방향 벡터/넓이 벡터: 어떤 곡선이나 곡면을 따라 적분하면 “특정 면적이나 방향”을 가진 벡터가 나온다고 해서 벡터 면적이라 불러.
•	이번 경우엔 $\int_0^1 r(t),dt$는 단순히 “곡선이 $t=0$에서 $t=1$까지 그려지는 동안, 각 좌표 성분이 누적되는 양”을 나타내고, 그게 $\langle 2\ln 2 -1, \tfrac{1}{2}, \tfrac{1}{3}\rangle$라는 벡터로 표현된 거야.

⸻

3. 구체적 해석

결과 벡터
$$
\langle 2\ln 2 - 1, \tfrac{1}{2}, \tfrac{1}{3} \rangle
$$
은 다음을 의미해:
•	$x$ 방향으로는 $\ln(1+t)$가 $[0,1]$ 구간에서 누적된 총량,
•	$y$ 방향으로는 $t$가 만들어낸 면적 (삼각형 면적처럼),
•	$z$ 방향으로는 $t^2$가 만든 면적이 합쳐져서,
공간 속에서 하나의 “평균적인 방향과 크기”를 가진 벡터가 나온 거야.

즉, 이 적분 결과는 그냥 수치가 아니라, 공간 곡선이 전체적으로 어떤 방향으로 누적되는지를 보여주는 벡터라고 보면 돼.

⸻

👉 정리하면, 여기서 “벡터 면적”은 면적의 실제 도형을 구하는 게 아니라, 각 성분의 적분값이 모여서 만들어지는 벡터를 뜻하고, 그건 곡선이 $[0,1]$ 구간 동안 전체적으로 어떻게 퍼져 있는지를 나타내는 지표라고 이해하면 돼.

오빠, 혹시 내가 이걸 그림(곡선과 적분 벡터를 3D로 표시)으로 시각화해줄까?





⸻