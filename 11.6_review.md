# 🧭 1. 방향 도함수 (Directional Derivative)

## 정의
함수 $f(x, y)$가 점 $(x_0, y_0)$에서 단위 벡터 $\mathbf{u} = \langle a, b \rangle$ 방향으로 변하는 순간 변화율은
$$D_{\mathbf{u}}f(x_0, y_0) = \lim_{h \to 0}\frac{f(x_0 + ha, y_0 + hb) - f(x_0, y_0)}{h}$$
으로 정의돼.

**미분 가능할 때의 계산 공식**
$$D_{\mathbf{u}}f(x, y) = f_x(x, y)a + f_y(x, y)b = \nabla f(x, y) \cdot \mathbf{u}$$

즉, 방향 도함수는 물매벡터와 방향벡터의 내적이야.

💡**직관**
어떤 방향으로 걸어갈 때 함수 값(예: 고도)이 얼마나 빠르게 바뀌는지를 말해줘.

**연습문제**
$f(x, y) = x^2y + 3y^2$일 때, 점 $(1, 2)$에서 $\mathbf{u} = \langle 3, 4 \rangle$ 방향의 방향도함수를 구하라.
(단, $\mathbf{u}$는 단위벡터로 바꿔서 계산할 것)

**풀이**

$\langle 3, 4 \rangle$의 단위 벡터는 $\langle 3/5, 4/5 \rangle$ 이다.

$$
\begin{align}
D_uf(x, y) &= \langle 2xy, x^2 + 6y \rangle \cdot \langle \frac{3}{5}, \frac{4}{5}\rangle \\
D_uf(1, 2) &= \langle 4, 13 \rangle \cdot \langle \frac{3}{5}, \frac{4}{5}\rangle \\
&= \frac{64}{5}
\end{align}
$$

“$(1, 2)$ 근처에서, 벡터 $\langle 3, 4\rangle$ 방향으로 아주 조금 움직이면
함수값이 그 거리 대비 12.8배 빠르게 증가한다”

⸻

# 🧩 2. 물매벡터 (Gradient Vector)

## 정의
$$\nabla f(x, y) = \langle f_x(x, y), f_y(x, y) \rangle$$
이는 함수 $f$의 기울기 벡터로, 각 좌표 방향의 변화율을 모은 벡터야.

**의미**
•	$\nabla f$의 방향: 함수가 가장 빠르게 증가하는 방향
•	$|\nabla f|$: 그 방향으로의 최대 증가율

**연습문제**
$f(x, y) = \sin(xy)$일 때, 점 $(\pi/2, 1)$에서 물매벡터 $\nabla f$를 구하고, 그 크기를 구하라.

**풀이**
$$
\begin{align}
\nabla f(x, y) &= \langle y\cos{(xy)}, x\cos{xy} \rangle \\
\nabla f(\pi/2, 1) &= \langle 0, 0\rangle \\
|\nabla f(\pi/2, 1)| &= 0
\end{align}
$$

⸻

# 🧗‍♀️ 3. 가장 가파른 방향 (최대 변화율)

## 정리
$$D_{\mathbf{u}}f = |\nabla f|\cos\theta$$
따라서 $D_{\mathbf{u}}f$가 최대가 되려면 $\cos\theta = 1$ → $\mathbf{u}$가 $\nabla f$와 같은 방향.

즉,

함수가 가장 빠르게 증가하는 방향은 $\nabla f$의 방향이고,
그때의 증가율은 $|\nabla f|$이다.

연습문제
$f(x, y) = e^{x - 2y}$일 때, 점 $(0, 0)$에서 함수가 가장 빨리 증가하는 방향 벡터와 그때의 최대 변화율을 구하라.

⸻

# 🌍 4. 등위 곡면의 접평면 (Level Surface & Tangent Plane)

## 개념
삼변수 함수 $F(x, y, z) = k$의 등위면 위의 점 $(x_0, y_0, z_0)$에서
접평면의 방정식은 다음과 같아:
$$F_x(x_0, y_0, z_0)(x - x_0) + F_y(x_0, y_0, z_0)(y - y_0) + F_z(x_0, y_0, z_0)(z - z_0) = 0$$

이때 법선 벡터는 $\nabla F(x_0, y_0, z_0)$이야.

**연습문제**
$F(x, y, z) = x^2 + 2y^2 + 3z^2$의 등위면 $F(x, y, z) = 24$ 위의 점 $(2, 2, 2)$에서
접평면의 방정식과 법선의 방향벡터를 구하라.

**풀이**

$$
\begin{align}
F_x &= 2x \\
F_y &= 4y \\
F_z &= 6z
\end{align}
$$

따라서 접평면의 방정식은 $2x_0(x - x_0) + 4y_0(y - y_0) + 6z_0(z - z_0) = 0$ 따라서 $(2,2,2)$에서의 접평면의 방정식은 $4(x - 2) + 8(y - 2) + 12(z - 2) = 0$ 즉, $x + 2y + 3z = 12$.

법선의 방향 벡터는 $\langle 1, 2, 3\rangle$이다.